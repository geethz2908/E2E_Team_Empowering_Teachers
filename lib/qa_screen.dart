import 'package:flutter/material.dart';

class QAScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Q&A')),
      body: Center(child: Text('This is the Q&A screen')),
    );
  }
}
